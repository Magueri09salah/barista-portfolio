# Photography

Drop image files here, then register the path in `lib/photos.ts`.

Naming and specs are listed in `/SHOTLIST.md` at the project root.

Until a slot has a `src`, the site renders a warm gradient placeholder —
so the layout is never broken by a missing photo.
